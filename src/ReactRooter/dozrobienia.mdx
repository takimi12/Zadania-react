1. dodanie *  w path i elementu gdzie ma przekierować w przypadku braku sciezki
2. <Route path="*" element={<Navigate to="."/>} />
3. const disable = true
if (disable) return >Navigate relative="path" to='.. />
4. const navigate = useNavigate();
useEffect(() => {
    setTimeout(() => {
        navigate('...', {
            relative: 'path'
        })
    }, 1000)
})